<div class="container">
  <h1 class="m-4">Your listings</h1>
  <%= link_to 'Add a Space', new_space_path, :class => "btn btn-primary m-4" %>
  <div class="row">
      <div class="col-6">
        <div class="d-flex align-content-start flex-wrap">
        <% @spaces.each do |space| %>
        <%= render 'shared/space_card1', space: space %>
        <% end %>
        </div>
    </div>
  </div>
</div>
